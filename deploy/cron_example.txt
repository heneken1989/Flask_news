# Cron job example for crawl_sections_multi_language.py
# Add this to crontab: crontab -e

# Chạy mỗi giờ (vào phút 0 của mỗi giờ)
0 * * * * cd /path/to/GC_HRAI/flask && /path/to/venv/bin/python scripts/crawl_sections_multi_language.py --section all >> /var/log/crawl_sections.log 2>&1

# Chạy mỗi 30 phút
*/30 * * * * cd /path/to/GC_HRAI/flask && /path/to/venv/bin/python scripts/crawl_sections_multi_language.py --section all >> /var/log/crawl_sections.log 2>&1

# Chạy mỗi 2 giờ
0 */2 * * * cd /path/to/GC_HRAI/flask && /path/to/venv/bin/python scripts/crawl_sections_multi_language.py --section all >> /var/log/crawl_sections.log 2>&1

# Chạy vào 8h sáng và 8h tối mỗi ngày
0 8,20 * * * cd /path/to/GC_HRAI/flask && /path/to/venv/bin/python scripts/crawl_sections_multi_language.py --section all >> /var/log/crawl_sections.log 2>&1

# Chạy vào 9h sáng mỗi ngày
0 9 * * * cd /path/to/GC_HRAI/flask && /path/to/venv/bin/python scripts/crawl_sections_multi_language.py --section all >> /var/log/crawl_sections.log 2>&1

# Chạy vào 3h sáng mỗi ngày (ít traffic)
0 3 * * * cd /path/to/GC_HRAI/flask && /path/to/venv/bin/python scripts/crawl_sections_multi_language.py --section all >> /var/log/crawl_sections.log 2>&1

